# Разбор PublishController

## Инструкция для кандидата

Проанализируй `app/Http/Controllers/PublishController.php` и опиши найденные проблемы.
Сколько их — реши сам. Для каждой проблемы заполни:

1. **Где:** строка или метод
2. **В чём суть:** что делается неправильно
3. **Последствие:** к чему это приводит
4. **Исправление:** как бы ты это переделал

---

## Пример формата (на постороннем коде, не из этого проекта)

### Проблема: отсутствие пагинации

**Где:** Метод `index()`, примерно строки 12–18.

**В чём суть:** Метод возвращает весь список сущностей без постраничной выборки.

**Последствие:** На большой таблице ответ и потребление памяти растут неограниченно; риск таймаута.

**Исправление:** Отдавать результат постранично (`paginate()`), возвращать метаданные страницы.

---

## Твой разбор

_Добавь столько блоков, сколько проблем нашёл._

### Проблема 1: (валидация)

**Где:** Строки 13-14

**В чём суть:** Ввод пользователя без валидации

**Последствие:** Проблема безопасности, publish_reason может содержать вредоносный код

**Исправление:** Валидация FormRequest

### Проблема 2: (N-запросов к БД)

**Где:** строки 18-19 `$post = Post::find($postId);`

**В чём суть:** Множественные запросы в БД

**Последствие:** Замедление скорости ответа, нагрузка на БД

**Исправление:** Найти посты одним запросом `Post::whereIn('id', $postIds)->get()`

### Проблема 3: (нарушение алгоритма)

**Где:** строки 25-26 `$shouldPublish = strlen($reason) > 10;`

**В чём суть:** Эта часть кода должна быть вне цикла

**Последствие:** Если этот параметр false, то код в принципе должен перескочить к ответу

**Исправление:** Вынести в валидацию

### Проблема 4: (валидация)

**Где:** строки 30-34 `$post->update([`

**В чём суть:** Очередной запуск пользовательского ввода в БД

**Последствие:** 

**Исправление:** Валидация

### Проблема 5: (+N запросов к БД)

**Где:** строка 37 `$commentCount = $post->comments()->count();`

**В чём суть:** Лишние запросы в БД

**Последствие:** 

**Исправление:** есть конструкция withCount для модели

### Проблема 6: (1+N+N запросов к БД)

**Где:** строки 63-64 `$user = $post->user; $comments = $post->comments;`

**В чём суть:** Лишние запросы в БД

**Последствие:** 

**Исправление:** Опять же методы with/withCount + не помещает проверка на существование пользователя

### P.S. не знаю на сколько вам актуально но `$score = $commentCount * 10 + strlen($post->body);` - подобные вещи я обычно выношу в отдельные сервис-классы

---

**Важно:** мы оцениваем твоё понимание, а не длину списка. Лучше меньше проблем, но разобранных по сути, чем много формальных строк.
